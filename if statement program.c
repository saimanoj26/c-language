#include <stdio.h>

int main ()
{
   int a = 40;

   if( a > 50 ) {

      printf("a is less than 50\n" );
    if (a<60)
       printf("a is less than 60\n")
   }
   printf("value of a is : %d\n", a);

   return 0;
}

